pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        flatDir {
            dirs 'libs'
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "HelloEarth"
include ':app'

include ':maply'
project(':maply').projectDir = new File(settingsDir, '../WhirlyGlobe/android/library/maply')
//project(':maply').projectDir = new File(settingsDir,"../../library/maply/")
//include ':maply'
